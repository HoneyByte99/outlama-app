When the user runs `/sync-docs`, update docs to match the current implementation.

Rules:
- prefer short, accurate, operational docs,
- remove stale claims,
- keep canonical truth in one place,
- update related rule/docs if a contract changed.
